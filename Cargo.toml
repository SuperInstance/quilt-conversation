[package]
name = "quilt-conversation"
version = "0.1.0"
edition = "2021"
description = "Inter-agent conversation as a live sequencer — t-minus simulation-first, sensor-confirmed pocket, internal iteration while others talk."
license = "MIT"
repository = "https://github.com/SuperInstance/quilt-conversation"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.150"
